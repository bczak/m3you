---
phase: 01-foundation-repair
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - tests/button.test.tsx
  - src/components/ui/navigation-bar.tsx
  - tests/navigation-bar.test.tsx
autonomous: true
requirements:
  - INFRA-01
  - INFRA-02

must_haves:
  truths:
    - "button.test.tsx elevated variant assertion checks shadow-lg (not shadow-md)"
    - "button.test.tsx outlined variant assertion checks border-outline/40 (not border-outline)"
    - "NavigationBarItem button element has aria-label={label} attribute"
    - "NavigationBarItem handleKeyDown fires onValueChange when Enter or Space is pressed and not disabled"
    - "navigation-bar.test.tsx aria-label assertions pass"
    - "navigation-bar.test.tsx keyboard activation tests pass"
  artifacts:
    - path: "tests/button.test.tsx"
      provides: "Corrected class assertions for elevated and outlined variants"
      contains: "shadow-lg"
    - path: "src/components/ui/navigation-bar.tsx"
      provides: "aria-label on button element and Enter/Space keyboard handler"
      contains: "aria-label={label}"
  key_links:
    - from: "tests/button.test.tsx"
      to: "src/components/ui/button.tsx CVA output"
      via: "toHaveClass assertion"
      pattern: "shadow-lg|border-outline/40"
    - from: "tests/navigation-bar.test.tsx"
      to: "src/components/ui/navigation-bar.tsx"
      via: "getByRole button with name + toHaveAttribute aria-label"
      pattern: "aria-label"
---

<objective>
Fix two stale class assertions in button.test.tsx and add missing aria-label attribute plus Enter/Space keyboard handling to NavigationBarItem source component.

Purpose: Eliminates the three failing test groups in button and navigation-bar test suites caused by class drift and missing accessibility implementation.
Output: Updated button.test.tsx (2 assertion changes), updated navigation-bar.tsx (aria-label + handleKeyDown), updated navigation-bar.test.tsx if any assertion needs correcting.
</objective>

<execution_context>
@/Users/monster/.claude/get-shit-done/workflows/execute-plan.md
@/Users/monster/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-repair/01-RESEARCH.md
@.planning/phases/01-foundation-repair/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix stale class assertions in button.test.tsx</name>
  <files>tests/button.test.tsx</files>
  <action>
Two test assertions in button.test.tsx assert class names that no longer match the component's CVA output. Update both to match the current output from src/components/ui/button.tsx:

1. In the 'applies elevated variant classes correctly' test:
   - Change: expect(button).toHaveClass('shadow-md')
   - To:     expect(button).toHaveClass('shadow-lg')

2. In the 'applies outlined variant classes correctly' test:
   - Change: expect(button).toHaveClass('border-outline')
   - To:     expect(button).toHaveClass('border-outline/40')

These are the ONLY changes to button.test.tsx. Do not change any other assertions. The component source (button.tsx) is already correct — only the test expectations are stale.

Run bun run check after editing to verify Biome passes.
  </action>
  <verify>grep -n "shadow-lg\|border-outline/40" tests/button.test.tsx</verify>
  <done>button.test.tsx has toHaveClass('shadow-lg') for elevated variant and toHaveClass('border-outline/40') for outlined variant; no other assertions changed</done>
</task>

<task type="auto">
  <name>Task 2: Add aria-label and Enter/Space handler to NavigationBarItem</name>
  <files>src/components/ui/navigation-bar.tsx</files>
  <action>
Two changes are needed in src/components/ui/navigation-bar.tsx in the NavigationBarItem component:

**Change 1 — Add aria-label to the button element:**
Find the button element rendered by NavigationBarItem. It currently has aria-current but no aria-label. Add aria-label={label} to the button element's props. The label prop is already in scope (it is a named prop of NavigationBarItem).

Place aria-label before aria-current so props remain sorted:
```tsx
<button
  ref={ref}
  type="button"
  aria-label={label}
  aria-current={isActive ? 'page' : undefined}
  disabled={disabled}
  ...
```

**Change 2 — Add Enter/Space logic to handleKeyDown:**
Find the handleKeyDown function in NavigationBarItem. It currently only forwards the event to props.onKeyDown. Replace it with:

```typescript
const handleKeyDown = (e: React.KeyboardEvent<HTMLButtonElement>) => {
  if ((e.key === 'Enter' || e.key === ' ') && !disabled) {
    onValueChange?.(value);
  }
  props.onKeyDown?.(e);
};
```

The variable names onValueChange, value, and disabled must match what is already destructured from props in the component. Read the file first to confirm the exact variable names in scope before editing.

Run bun run check after editing to verify Biome is satisfied (sorted Tailwind classes, no non-null assertions, SVG accessibility).
  </action>
  <verify>grep -n "aria-label={label}" src/components/ui/navigation-bar.tsx && grep -n "e.key === 'Enter'" src/components/ui/navigation-bar.tsx</verify>
  <done>navigation-bar.tsx has aria-label={label} on the NavigationBarItem button element and handleKeyDown fires onValueChange on Enter/Space when not disabled</done>
</task>

</tasks>

<verification>
Run bun run test -- --reporter=verbose and confirm:
- All button.test.tsx tests pass (0 failures)
- All navigation-bar.test.tsx tests pass (0 failures, including aria-label and keyboard activation tests)
</verification>

<success_criteria>
- button.test.tsx: 0 failures (elevated shadow-lg and outlined border-outline/40 assertions pass)
- navigation-bar.test.tsx: 0 failures (aria-label attribute present on items, Enter/Space activates items)
- bun run check passes with no Biome violations
- No other tests broken (existing passing tests unaffected)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-repair/01-02-SUMMARY.md` using the summary template.
</output>
