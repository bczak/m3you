---
phase: 01-foundation-repair
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/components/ui/navigation-rail.tsx
autonomous: true
requirements:
  - INFRA-03

must_haves:
  truths:
    - "NavigationRailItem collapsed branch button element has aria-label={label} attribute"
    - "NavigationRailItem handleKeyDown fires onValueChange when Enter or Space is pressed and not disabled"
    - "navigation-rail.test.tsx click tests no longer crash with animate errors (polyfill from Plan 01 handles this)"
    - "All navigation-rail.test.tsx tests pass"
  artifacts:
    - path: "src/components/ui/navigation-rail.tsx"
      provides: "aria-label on collapsed branch button and Enter/Space keyboard handler"
      contains: "aria-label={label}"
  key_links:
    - from: "tests/navigation-rail.test.tsx"
      to: "src/components/ui/navigation-rail.tsx collapsed branch"
      via: "getByRole button with name + toHaveAttribute aria-label"
      pattern: "aria-label"
    - from: "tests/navigation-rail.test.tsx"
      to: "NavigationRailItem handleKeyDown"
      via: "fireEvent.keyDown + onValueChange callback"
      pattern: "e.key === 'Enter'"
---

<objective>
Fix NavigationRailItem source component: add aria-label to the collapsed branch button element and implement Enter/Space keyboard activation in handleKeyDown.

Purpose: Eliminates the remaining navigation-rail.test.tsx failures. The animate crash is already resolved by Plan 01's global polyfill. This plan addresses the two source-level gaps specific to NavigationRailItem.
Output: Updated navigation-rail.tsx with aria-label on collapsed branch and working handleKeyDown.
</objective>

<execution_context>
@/Users/monster/.claude/get-shit-done/workflows/execute-plan.md
@/Users/monster/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-repair/01-RESEARCH.md
@.planning/phases/01-foundation-repair/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add aria-label and Enter/Space handler to NavigationRailItem</name>
  <files>src/components/ui/navigation-rail.tsx</files>
  <action>
Two changes are needed in NavigationRailItem inside src/components/ui/navigation-rail.tsx:

**Change 1 — Add aria-label to the collapsed branch button:**
NavigationRailItem has two render branches: collapsed and expanded. The expanded branch already has aria-label={label}. The collapsed branch does not. Find the collapsed branch (the button element rendered when the parent NavigationRail is in collapsed/non-expanded state) and add aria-label={label} to its props.

Place aria-label before aria-current in the props list (consistent ordering with the expanded branch and with the NavigationBarItem fix in Plan 02):
```tsx
<button
  ref={ref}
  type="button"
  aria-label={label}
  aria-current={isActive ? 'page' : undefined}
  disabled={disabled}
  ...
```

Read the file first to locate exactly which lines form the collapsed branch return and verify the label prop name in scope.

**Change 2 — Add Enter/Space logic to handleKeyDown:**
Find the handleKeyDown function in NavigationRailItem. It currently only forwards the event to props.onKeyDown (or is a passthrough). Replace it with the same pattern as NavigationBarItem:

```typescript
const handleKeyDown = (e: React.KeyboardEvent<HTMLButtonElement>) => {
  if ((e.key === 'Enter' || e.key === ' ') && !disabled) {
    onValueChange?.(value);
  }
  props.onKeyDown?.(e);
};
```

Confirm the exact variable names (onValueChange, value, disabled) match what is destructured in NavigationRailItem before editing. The pattern is identical to NavigationBarItem — same keyboard contract, same ARIA interaction model.

Run bun run check after editing to verify Biome passes (sorted Tailwind classes, SVG aria-hidden on decorative icons, no non-null assertions).
  </action>
  <verify>grep -n "aria-label={label}" src/components/ui/navigation-rail.tsx && grep -n "e.key === 'Enter'" src/components/ui/navigation-rail.tsx</verify>
  <done>navigation-rail.tsx collapsed branch has aria-label={label} on the button element and handleKeyDown fires onValueChange on Enter/Space when not disabled; both the collapsed and expanded branches are consistent</done>
</task>

</tasks>

<verification>
Run bun run test -- --reporter=verbose and confirm:
- All navigation-rail.test.tsx tests pass (0 failures)
- No regressions in other test files

Then run bun run test for the full suite and confirm 0 total failures across button.test.tsx, navigation-bar.test.tsx, and navigation-rail.test.tsx.
</verification>

<success_criteria>
- navigation-rail.test.tsx: 0 failures (aria-label attribute present on collapsed item buttons, Enter/Space activates items, click tests no longer crash)
- bun run check passes with no Biome violations
- Full bun run test shows 0 failures in the three previously-failing test files
- No other tests broken (time-picker.test.tsx also benefits from Plan 01's global polyfill as a side effect)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-repair/01-03-SUMMARY.md` using the summary template.
</output>
