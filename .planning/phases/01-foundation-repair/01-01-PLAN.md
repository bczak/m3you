---
phase: 01-foundation-repair
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - rstest.setup.ts
  - tests/navigation-bar.test.tsx
  - tests/navigation-rail.test.tsx
  - tests/time-picker.test.tsx
autonomous: true
requirements:
  - INFRA-04
  - INFRA-05

must_haves:
  truths:
    - "rstest.setup.ts contains a global Element.prototype.animate polyfill at top level"
    - "rstest.setup.ts registers a global afterEach(cleanup) call"
    - "No individual test file registers its own afterEach(cleanup) (removed from nav-bar, nav-rail, time-picker)"
    - "menu.test.tsx beforeAll polyfill is still present (not removed — menu.test.tsx is not in scope)"
  artifacts:
    - path: "rstest.setup.ts"
      provides: "Global animate polyfill and cleanup registration"
      contains: "Element.prototype.animate"
    - path: "rstest.setup.ts"
      provides: "Global afterEach cleanup"
      contains: "afterEach"
  key_links:
    - from: "rstest.setup.ts"
      to: "all test files"
      via: "rstest.config.ts setupFiles entry"
      pattern: "setupFiles"
---

<objective>
Centralise global test infrastructure into rstest.setup.ts: add the Element.prototype.animate polyfill and the afterEach(cleanup) call so no individual test file needs to carry either.

Purpose: Makes the test suite self-healing — new test files cannot accidentally omit the polyfill or cleanup. Directly unblocks navigation-rail tests that crash without the animate polyfill.
Output: Updated rstest.setup.ts with two new global registrations; per-file cleanup registrations removed from three test files.
</objective>

<execution_context>
@/Users/monster/.claude/get-shit-done/workflows/execute-plan.md
@/Users/monster/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-repair/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add global polyfill and cleanup to rstest.setup.ts</name>
  <files>rstest.setup.ts</files>
  <action>
Add two global registrations to rstest.setup.ts after the existing expect.extend(jestDomMatchers) line:

1. Import afterEach from '@rstest/core' and cleanup from '@testing-library/react' at the top of the file (join the existing import from '@rstest/core').

2. Add the animate polyfill block at top level (not inside beforeAll — top-level code in setupFiles runs globally before any test):

```typescript
// Global polyfill: happy-dom does not implement Web Animations API
// m3-ripple calls element.animate() on pointer events
if (typeof Element !== 'undefined' && !Element.prototype.animate) {
  Element.prototype.animate = () =>
    ({ onfinish: null, cancel: () => {}, finished: Promise.resolve() }) as unknown as Animation;
}
```

3. Add the global cleanup registration after the polyfill:

```typescript
// Global cleanup: prevent DOM leakage between tests
afterEach(() => {
  cleanup();
});
```

The final file should have: imports, expect.extend, polyfill guard, afterEach cleanup — in that order.

Run bun run check after editing to verify Biome passes (sorted imports, etc.).
  </action>
  <verify>grep -n "Element.prototype.animate" rstest.setup.ts && grep -n "afterEach" rstest.setup.ts</verify>
  <done>rstest.setup.ts contains the animate polyfill guard block and an afterEach(cleanup) call at top level</done>
</task>

<task type="auto">
  <name>Task 2: Remove per-file afterEach(cleanup) from three test files</name>
  <files>
    tests/navigation-bar.test.tsx
    tests/navigation-rail.test.tsx
    tests/time-picker.test.tsx
  </files>
  <action>
In each of the three test files, remove the per-file afterEach(cleanup) registration to avoid double-cleanup now that it is global. Follow this exact procedure for each file:

1. Remove the afterEach(() => { cleanup(); }) call and its preceding comment (if any).
2. Remove 'afterEach' from the @rstest/core import if it is no longer used for anything else in that file. If afterEach is still used for other things in the file, leave the import.
3. Remove 'cleanup' from the @testing-library/react import if it is no longer imported by anything else in that file.

Do NOT modify menu.test.tsx — that file is not in scope for this plan (it has a beforeAll polyfill pattern that is redundant after this plan but harmless, and will be addressed separately if needed).

Run bun run check after edits to verify Biome is satisfied.
  </action>
  <verify>bun run check</verify>
  <done>None of the three test files (navigation-bar.test.tsx, navigation-rail.test.tsx, time-picker.test.tsx) contain an afterEach(cleanup) call; Biome check passes with no errors</done>
</task>

</tasks>

<verification>
Run bun run test — the test count should be the same (no tests removed), and the navigate-rail tests that previously crashed with animate errors may now show different failures (aria-label and keyDown failures instead of animate crashes — those are fixed in Plan 03).
</verification>

<success_criteria>
- rstest.setup.ts has both the animate polyfill guard and the global afterEach(cleanup)
- tests/navigation-bar.test.tsx, tests/navigation-rail.test.tsx, and tests/time-picker.test.tsx do not register afterEach(cleanup) individually
- bun run check passes with no Biome violations
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-repair/01-01-SUMMARY.md` using the summary template.
</output>
